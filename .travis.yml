language: node_js

sudo: false

cache: 
  apt: true
  directories: 
    - node_modules

node_js: stable

install: 
  - npm install -D
  - cd ./test/smoke/template
  - npm install -D
  # - cd ../../../ 老师的需要加这个，但是我没加这个也运行OK

script: 
  - npm test
